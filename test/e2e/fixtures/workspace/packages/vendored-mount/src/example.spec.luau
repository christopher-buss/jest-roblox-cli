-- Placeholder spec; the fake Open Cloud server returns canned envelopes
-- regardless of place contents. This file only needs to match the
-- workspace runner's testMatch glob so the runner reaches backend dispatch.
return {}
