-- Placeholder spec — never executed because the e2e test uses the fake
-- Open Cloud server which returns canned envelopes regardless of the
-- uploaded place contents. Just needs to match the testMatch glob so the
-- multi-project runner reaches backend dispatch.
return {}
